const ArrowIcon = ({className = ""}) => {
    return (
        <span className={`material-symbols-outlined ${className}`}>
            arrow_right_alt
        </span>
    )
}

export default ArrowIcon